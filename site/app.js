const USER="RifatNSU701",DATA="./data";const $=s=>document.querySelector(s);const esc=s=>String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const profile=await fetch(`${DATA}/profile.json`).then(r=>r.json());let gh=await fetch(`${DATA}/github.json`).then(r=>r.ok?r.json():{}).catch(()=>({}));let contrib=await fetch(`${DATA}/contributions.json`).then(r=>r.ok?r.json():{}).catch(()=>({}));
function renderProfile(){
$("#tagline").textContent=profile.tagline;$("#aboutText").textContent=profile.about;$("#degree").textContent=profile.education.degree;$("#institution").textContent=profile.education.institution;$("#eduStatus").textContent=profile.education.status;$("#currentlyList").innerHTML=profile.currently.map(x=>`<div class="current-item">${esc(x)}</div>`).join("");$("#focusList").innerHTML=profile.focus.slice(0,5).map(x=>`<div class="check">Focused on ${esc(x)}</div>`).join("");$("#skillCount").textContent=`${profile.skills.length} CAPABILITIES`;
const cats=[...new Set(profile.skills.map(x=>x[0]))];$("#skillTabs").innerHTML=["ALL",...cats].map((x,i)=>`<button class="skill-tab ${i===0?"active":""}" data-cat="${esc(x)}">${esc(x)}</button>`).join("");const drawSkills=(cat="ALL")=>$("#skillGrid").innerHTML=profile.skills.filter(x=>cat==="ALL"||x[0]===cat).map(x=>`<div class="skill"><div class="mono">${esc(x[2])}</div><strong>${esc(x[1])}</strong><small>${esc(x[0])}</small></div>`).join("");drawSkills();$("#skillTabs").addEventListener("click",e=>{if(e.target.matches(".skill-tab")){document.querySelectorAll(".skill-tab").forEach(b=>b.classList.remove("active"));e.target.classList.add("active");drawSkills(e.target.dataset.cat)}});
const toolNames=["Kali Linux","Wireshark","Wazuh","Splunk","Elastic","Metasploit","Nmap","Burp Suite","pfSense","Snort"];$("#toolGrid").innerHTML=toolNames.map(n=>{const s=profile.skills.find(x=>x[1]===n);return`<div class="tool"><span class="mono">${esc(s?.[2]||n.slice(0,3).toUpperCase())}</span><span>${esc(n)}</span></div>`}).join("");$("#experienceList").innerHTML=profile.experience.map(x=>`<div class="timeline-item"><h3>${esc(x.role)}</h3><div class="company">${esc(x.company)}</div><div class="period">${esc(x.period)}</div><p>${esc(x.description)}</p></div>`).join("");$("#certList").innerHTML=profile.certifications.map(x=>`<div class="cert"><div class="cert-badge">${esc(x.issuer.split(/[ /]/)[0].slice(0,4).toUpperCase())}</div><div><h3>${esc(x.name)}</h3><p>${esc(x.issuer)}${x.date?` · ${esc(x.date)}`:""}</p><span class="status">${esc(x.status)}</span></div></div>`).join("");$("#orgList").innerHTML=profile.organizations.map(x=>`<div class="org"><div class="org-icon">◈</div><div><h3>${esc(x.name)}</h3><p>${esc(x.detail)}</p></div><a href="${esc(x.url)}" target="_blank" rel="noreferrer">↗</a></div>`).join("");$("#contactGrid").innerHTML=profile.socials.map(x=>`<a class="contact" href="${esc(x.url)}" target="_blank" rel="noreferrer"><div class="contact-icon">${esc(x.icon)}</div><div><strong>${esc(x.label)}</strong><span>${esc(x.value)}</span></div></a>`).join("")}
renderProfile();
async function liveGitHub(){const[u,r,e]=await Promise.all([fetch(`https://api.github.com/users/${USER}`).then(x=>x.ok?x.json():null),fetch(`https://api.github.com/users/${USER}/repos?per_page=100&sort=updated`).then(x=>x.ok?x.json():[]),fetch(`https://api.github.com/users/${USER}/events/public?per_page=15`).then(x=>x.ok?x.json():[])]);if(!u)return gh;gh={...gh,user:u,repos:r,events:e,source:"live"};return gh}try{gh=await liveGitHub();$("#syncStatus").textContent="LIVE"}catch{$("#syncStatus").textContent="CACHE"}
const user=gh.user||{},repos=Array.isArray(gh.repos)?gh.repos:[],events=Array.isArray(gh.events)?gh.events:[];const stars=repos.reduce((n,r)=>n+(r.stargazers_count||0),0),forks=repos.reduce((n,r)=>n+(r.forks_count||0),0);$("#repoCount").textContent=user.public_repos??gh.public_repos??"—";$("#starCount").textContent=stars;$("#forkCount").textContent=forks;$("#followers").textContent=user.followers??"—";$("#sideFollowers").textContent=user.followers??"—";$("#sideFollowing").textContent=user.following??"—";
const featured=profile.featuredOverride.map(n=>repos.find(r=>r.name===n)).filter(Boolean),chosen=featured.length?featured:repos.filter(r=>!r.fork).sort((a,b)=>(b.stargazers_count-a.stargazers_count)||(new Date(b.pushed_at)-new Date(a.pushed_at))).slice(0,6);$("#projectMeta").textContent=`${chosen.length} SELECTED / ${repos.length} PUBLIC`;$("#projectGrid").innerHTML=chosen.map(r=>`<article class="project"><div class="project-top"><div class="project-icon">${esc((r.language||"GH").slice(0,3).toUpperCase())}</div><h3>${esc(r.name)}</h3></div><p>${esc(r.description||"Repository with no public description.")}</p><div class="badges"><span class="badge">${esc(r.language||"Mixed")}</span>${(r.topics||[]).slice(0,2).map(t=>`<span class="badge">${esc(t)}</span>`).join("")}</div><div class="project-foot"><span>★ ${r.stargazers_count||0} &nbsp;⑂ ${r.forks_count||0}</span><a href="${esc(r.html_url)}" target="_blank" rel="noreferrer">VIEW ↗</a></div></article>`).join("")||`<div class="muted">No public repositories returned by the API.</div>`;
function eventText(ev){const repo=ev.repo?.name||"GitHub",t=ev.type||"",map={PushEvent:"Pushed commits",WatchEvent:"Starred",ForkEvent:"Forked",CreateEvent:"Created",IssuesEvent:"Updated an issue",PullRequestEvent:"Updated a pull request",PullRequestReviewEvent:"Reviewed a pull request",ReleaseEvent:"Published a release"};return`${map[t]||t.replace("Event","")} <a href="https://github.com/${esc(repo)}" target="_blank" rel="noreferrer">${esc(repo.split("/").pop())}</a>`}$("#activityList").innerHTML=events.slice(0,7).map(ev=>`<div class="activity"><span class="activity-icon">${ev.type==="PushEvent"?"↥":"◉"}</span><span><b>${eventText(ev)}</b></span><time>${timeAgo(ev.created_at)}</time></div>`).join("")||`<div class="muted">No public events available.</div>`;function timeAgo(d){if(!d)return"—";const s=(Date.now()-new Date(d))/1000;if(s<60)return"now";if(s<3600)return`${Math.floor(s/60)}m`;if(s<86400)return`${Math.floor(s/3600)}h`;return`${Math.floor(s/86400)}d`}
const langAgg={};if(gh.languages)Object.assign(langAgg,gh.languages);else repos.forEach(r=>{if(r.language)langAgg[r.language]=(langAgg[r.language]||0)+1});const langEntries=Object.entries(langAgg).sort((a,b)=>b[1]-a[1]).slice(0,6),langTotal=langEntries.reduce((a,b)=>a+b[1],0)||1;$("#languageList").innerHTML=langEntries.map(([n,v])=>`<div class="lang"><div class="lang-head"><span>${esc(n)}</span><b>${Math.round(v/langTotal*100)}%</b></div><div class="lang-bar"><i style="width:${Math.max(3,Math.round(v/langTotal*100))}%"></i></div></div>`).join("")||`<div class="muted">Language data will appear after synchronization.</div>`;
function renderContrib(data){const days=Array.isArray(data.days)?data.days:[],cells=days.slice(-371);$("#heatGrid").innerHTML="";const wrap=document.createElement("div");wrap.className="heat-grid";wrap.style.gridTemplateRows="repeat(7,14px)";wrap.style.gridAutoFlow="column";cells.forEach(d=>{const c=document.createElement("span");c.className="heat-cell "+(d.count>=12?"l4":d.count>=7?"l3":d.count>=3?"l2":d.count>=1?"l1":"");c.title=`${d.date||""}: ${d.count||0} contributions`;wrap.appendChild(c)});$("#heatGrid").appendChild(wrap);$("#contribTotal").textContent=data.total!=null?`${data.total} CONTRIBUTIONS`:"DATA VIA ACTIONS";$("#streak").textContent=data.currentStreak!=null?`STREAK ${data.currentStreak} DAYS`:"STREAK —"}renderContrib(contrib);$("#months").innerHTML=["JAN","FEB","MAR","APR","MAY","JUN","JUL","AUG","SEP","OCT","NOV","DEC"].map(m=>`<span>${m}</span>`).join("");
function terminal(){const lines=["$ whoami","rifat-khan","$ focus","cybersecurity / networking / infrastructure","$ status","building secure systems..."];let i=0;const body=$("#terminalBody"),type=()=>{if(i>=lines.length){body.innerHTML+=`<span class="terminal-cursor"></span>`;return}const line=lines[i++],el=document.createElement("div");body.appendChild(el);let j=0;const tick=()=>{el.textContent=line.slice(0,j++);if(j<=line.length)setTimeout(tick,line.startsWith("$")?42:22);else setTimeout(type,220)};tick()};type()}terminal();
function setupCanvas(c){const p=c.parentElement,ctx=c.getContext("2d");let w=0,h=0,dpr=1,raf=0,active=true;const resize=()=>{dpr=Math.min(devicePixelRatio||1,2);w=p.clientWidth;h=p.clientHeight;c.width=w*dpr;c.height=h*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)};resize();addEventListener("resize",resize);new IntersectionObserver(es=>{active=es[0].isIntersecting;if(active)raf=requestAnimationFrame(draw)}).observe(c);return{ctx,p,get w(){return w},get h(){return h},active:()=>active,raf:v=>raf=v}}
function world(){const c=$("#worldCanvas"),s=setupCanvas(c),ctx=s.ctx;const continents=[[[.18,.32],[.24,.22],[.34,.25],[.39,.34],[.34,.41],[.29,.47],[.25,.60],[.20,.54],[.23,.44],[.16,.39]],[[.43,.58],[.48,.64],[.50,.76],[.46,.91],[.40,.83],[.39,.70]],[[.54,.28],[.62,.22],[.72,.25],[.79,.34],[.75,.42],[.66,.39],[.61,.48],[.55,.41]],[[.74,.50],[.84,.49],[.90,.59],[.86,.67],[.76,.63],[.71,.56]],[[.60,.58],[.68,.56],[.72,.65],[.68,.77],[.61,.82],[.56,.72]]];const cities=Array.from({length:34},(_,i)=>({x:.18+((i*47)%65)/100,y:.18+((i*29)%62)/100}));function draw(t){if(!s.active())return;const w=s.w,h=s.h;ctx.clearRect(0,0,w,h);const cx=w*.55,cy=h*.49,r=Math.min(w*.38,h*.40,180);ctx.save();ctx.translate(cx,cy);ctx.beginPath();ctx.arc(0,0,r,0,Math.PI*2);ctx.fillStyle="rgba(0,35,52,.34)";ctx.fill();ctx.clip();for(let i=-4;i<=4;i++){ctx.strokeStyle="rgba(0,213,255,.18)";ctx.lineWidth=1;ctx.beginPath();ctx.ellipse(0,0,r*.25+Math.abs(i)*r*.19,r,0,0,Math.PI*2);ctx.stroke()}for(let i=-3;i<=3;i++){ctx.beginPath();ctx.ellipse(0,0,r,r*.28+Math.abs(i)*r*.16,0,0,Math.PI*2);ctx.stroke()}continents.forEach(poly=>{ctx.beginPath();poly.forEach(([x,y],i)=>{const px=(x-.55)*r*2,py=(y-.5)*r*2;i?ctx.lineTo(px,py):ctx.moveTo(px,py)});ctx.closePath();ctx.fillStyle="rgba(0,164,214,.16)";ctx.strokeStyle="rgba(0,217,255,.55)";ctx.fill();ctx.stroke()});cities.forEach((q,i)=>{const x=(q.x-.55)*r*2,y=(q.y-.5)*r*2;ctx.fillStyle=i%7===0?"#00f5a0":"#00d9ff";ctx.shadowBlur=8;ctx.shadowColor=ctx.fillStyle;ctx.beginPath();ctx.arc(x,y,1.4+(i%3)*.5,0,7);ctx.fill();ctx.shadowBlur=0;if(i%6===0){ctx.strokeStyle="rgba(0,245,160,.28)";ctx.beginPath();ctx.arc(x,y,7+Math.sin(t*.003+i)*3,0,7);ctx.stroke()}});for(let i=0;i<10;i++){const a=(i*2.7+t*.00025)%6.28,b=a+.75;ctx.strokeStyle="rgba(0,245,160,.45)";ctx.beginPath();ctx.arc(0,0,r+6,a,b);ctx.stroke()}ctx.restore();ctx.strokeStyle="rgba(0,217,255,.55)";ctx.lineWidth=1.2;ctx.beginPath();ctx.arc(cx,cy,r+9,Math.sin(t*.00025),Math.sin(t*.00025)+1.05);ctx.stroke();ctx.strokeStyle="rgba(0,217,255,.10)";ctx.beginPath();ctx.arc(cx,cy,r+18,0,Math.PI*2);ctx.stroke();s.raf(requestAnimationFrame(draw))}s.raf(requestAnimationFrame(draw))}world();
function network(){const c=$("#networkCanvas"),s=setupCanvas(c),ctx=s.ctx;const names=["CLIENT","FIREWALL","SOC","SIEM","CLOUD","DATABASE","ENDPOINT","NMAP","SENSOR","SERVER","VPN","IDS"];const nodes=names.map((name,i)=>({name,x:.08+((i*43)%84)/100,y:.18+((i*67)%68)/100}));const edges=[[0,1],[1,2],[2,3],[3,4],[3,5],[4,6],[6,7],[2,8],[8,9],[5,9],[1,10],[10,11],[11,2]];function draw(t){if(!s.active())return;const w=s.w,h=s.h;ctx.clearRect(0,0,w,h);ctx.strokeStyle="rgba(0,183,255,.06)";for(let x=0;x<w;x+=32){ctx.beginPath();ctx.moveTo(x,0);ctx.lineTo(x,h);ctx.stroke()}for(let y=0;y<h;y+=28){ctx.beginPath();ctx.moveTo(0,y);ctx.lineTo(w,y);ctx.stroke()}edges.forEach(([a,b],ei)=>{const A=nodes[a],B=nodes[b],ax=A.x*w,ay=A.y*h,bx=B.x*w,by=B.y*h;ctx.strokeStyle=ei%4===0?"rgba(0,245,160,.34)":"rgba(0,217,255,.24)";ctx.beginPath();ctx.moveTo(ax,ay);ctx.lineTo(bx,by);ctx.stroke();for(let k=0;k<2;k++){const q=(t*.00016+ei*.17+k*.5)%1,x=ax+(bx-ax)*q,y=ay+(by-ay)*q;ctx.fillStyle=k?"#00d9ff":"#00f5a0";ctx.shadowBlur=9;ctx.shadowColor=ctx.fillStyle;ctx.beginPath();ctx.arc(x,y,2.1,0,7);ctx.fill();ctx.shadowBlur=0}});nodes.forEach((n,i)=>{const x=n.x*w,y=n.y*h,active=i===2||i===3;ctx.fillStyle="rgba(2,18,27,.95)";ctx.strokeStyle=active?"#00f5a0":"#00d9ff";ctx.lineWidth=1.4;ctx.beginPath();ctx.arc(x,y,active?11:8,0,7);ctx.fill();ctx.stroke();if(active){ctx.strokeStyle="rgba(0,245,160,.2)";ctx.beginPath();ctx.arc(x,y,18+Math.sin(t*.003+i)*3,0,7);ctx.stroke()}ctx.fillStyle="#8eb0bc";ctx.font="8px JetBrains Mono,monospace";ctx.fillText(n.name,x+14,y+3)});s.raf(requestAnimationFrame(draw))}s.raf(requestAnimationFrame(draw))}network();